{
  "name": "bait-alkabsa-app",
  "version": "1.0.0",
  "private": true,
  "description": "تطبيق جوال لمطعم بيت الكبسة — تغليف الموقع بـ Capacitor",
  "type": "module",
  "scripts": {
    "test": "node test-web.mjs",
    "sync-web": "node sync-web.mjs",
    "add:android": "npm run sync-web && cap add android",
    "sync": "npm run sync-web && cap sync",
    "open:android": "cap open android",
    "build:apk": "npm run sync && cd android && gradlew.bat assembleDebug",
    "build:release": "npm run sync && cd android && gradlew.bat bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/splash-screen": "^6.0.2",
    "@capacitor/status-bar": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "jsdom": "^30.0.1"
  }
}
